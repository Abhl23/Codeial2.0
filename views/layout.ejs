<html>
  <head>
    <title><%= title %></title>
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css"
    />
    <script
      type="text/javascript"
      src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"
    ></script>
    <script
      type="text/javascript"
      src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.0/jquery.min.js"
    ></script>

    <link rel="stylesheet" type="text/css" href=<%= "/css/" + assetPath("layout.css") %> />
    <link rel="stylesheet" type="text/css" href=<%= "/css/" + assetPath("header.css") %> />
    <link rel="stylesheet" type="text/css" href=<%= "/css/" + assetPath("footer.css") %> />

    <%- style %>
  </head>
  <body>
    <%- include('_header'); %>

    <!-- <% if(flash.success && flash.success.length > 0) { %>
            <h3>
                <%= flash.success %>
            </h3>    
        <% } %> -->

    <main id="layout-main">
      <%- body %>
    </main>

    <%- include('_footer'); %>
    
    <%- script %>

    <% if(flash.success && flash.success.length > 0) { %>
        <script>
          new Noty({
            type: "success",
            text: "<%= flash.success %>",
            theme: "relax",
            layout: "topRight",
            timeout: 1500
          }).show();
        </script>
    <% } %>

    <% if(flash.error && flash.error.length > 0) { %>
        <script>
          new Noty({
            type: "error",
            text: "<%= flash.error %>",
            theme: "relax",
            layout: "topRight",
            timeout: 1500
          }).show();
        </script>
    <% } %>
  </body>
</html>
